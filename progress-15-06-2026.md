# Progress — 15/06/2026

Naming convention going forward: each session's notes go in `progress-DD-MM-YYYY.md` (date written as `DD/MM/YYYY` in the heading).

## Button (Large): badge controls + dark mode

### Done
- **Badge Playground controls** (`src/components/button/Button.stories.jsx`)
  - Added `showBadge` (True/False toggle) and `badgeValue` (editable text) args.
  - Conditional `argTypes.if` rules enforce the content-slot rules: `showLeftIcon`/`showRightIcon` hide when `showBadge` is on; `showBadge` hides when `onlyIcon` is on.
  - Content-slot precedence (unchanged, already in `Button.jsx`): `onlyIcon` → icon only; `badge` set → label + badge only (icons suppressed); otherwise → leftIcon? + label + rightIcon?.

- **Dark mode** (`src/design-tokens/tokens.css`)
  - Added a `[data-theme="dark"]` override block with dark equivalents for every `--button-*` token (fill/stroke/lighter/ghost/disabled/focus-ring) plus `--color-surface-neutral-soft` / `--color-text-neutral-strong` (badge) and dark `--shadow-focus-ring-*` values (swapped the `#ffffff` ring-offset for `#161616`).
  - **This is a draft palette** — not yet verified against the Figma "theme" variable collection (Light/Dark modes, 149 vars) the user created. Figma extraction was blocked this session (Dev Mode MCP metadata too large / no color values; Desktop Bridge console MCP not connected). **Next time**: either get the user to paste exact dark hex values for the `theme` collection, or connect the Figma Desktop Bridge plugin so `mcp__figma__figma_get_variables` can resolve Dark-mode values directly.

- **Storybook theme toggle** (`.storybook/preview.js`)
  - Added a `globalTypes.theme` toolbar item (☀ Light / 🌙 Dark).
  - Decorator wraps every story in a div with `data-theme` + matching background (`#ffffff` light / `#161616` dark), and centers story content (`display:flex; align-items:center; justify-content:center`).
  - Verified: Playground and AllVariants both render correctly in light + dark.

### Open items / carry-over
1. Dark palette is a draft — confirm against Figma `theme` collection (Light/Dark) values when available.
2. Consider whether `[data-theme="dark"]` should also get dark variants of other component families (badge tokens done for Button; other components not yet touched).

## Component status
- **Button (Large)** — built, documented (`button-large-spec.md`), Storybook stories (Playground + AllVariants), light/dark mode done (pending Figma palette verification).
- **Button (Medium/Small)** — not started (`size` prop reserved).
- Other components — not started.

## Next session
- Pick the next component to build (per the project's "Audit existing components" / design-to-dev workflow). Apply the same pattern: spec → component (CSS vars only) → Storybook story → dark-mode tokens + verification.
