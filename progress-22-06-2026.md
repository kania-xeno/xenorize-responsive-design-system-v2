# Progress — 22/06/2026

## Token sync — Design System Scalable V.2.1.0

Pulled all 1,317 variables from Figma (Desktop Bridge connected) and diffed against our `tokens.css`. Applied all changes below.

### Changes to `src/design-tokens/tokens.css`

**Fixed values (light mode):**
- `--button-focus-ring-primary`: `#7963ba3d` → `#513ebe3d` (corrected to actual `state/focus-ring-primary` alpha)

**New light-mode tokens added:**
- Active states: `--button-fill-primary-bg-active: #513ebe`, `--button-fill-destructive-bg-active: #941010`, `--button-fill-neutral-bg-active: #161616`
- Secondary fill type (teal/skableoff brand): `--button-fill-secondary-*`, `--button-ghost-secondary-*`
- Disabled border: `--button-disabled-border: #eaeaea`
- **Outline variant** (primary + secondary): `--button-outline-*` tokens — border-only, transparent bg
- **Tonal variant** (primary + secondary): `--button-tonal-*` tokens — tinted bg with active state
- Secondary focus ring: `--button-focus-ring-secondary: #22d3bb3d`

**Dark mode block — replaced draft with exact Figma values:**
- `background/base` dark: `#1b1c22` (was guessing `#161616`)
- `surface/neutral/soft` dark: `#2d2f39` (was `#333333`)
- Primary dark fills resolved from `brand/primary/*` dark mode values
- Destructive dark from `status/danger/*` dark mode values
- Neutral dark from `surface/neutral/sub/strong-900/strong-950` dark
- Full secondary type + outline + tonal dark values added
- Shadow ring offset corrected: `#161616` → `#1b1c22`

**Fixed `.storybook/preview.js`:**
- Decorator background dark: `#161616` → `#1b1c22` (matching `background/base` dark token)

### What changed in Figma (V.2.1.0) vs previous session

New in the design system since last session:
- **Secondary brand** (skableoff/teal) added across all button variants
- `button/outline/*` — new variant (same visual as stroke but different token namespace)
- `button/tonal/*` — new variant (like lighter but with active state)
- Active states (`bg-active`) on fill variants
- `button/disabled/border` token added
- 813 component variables total (was 770)

### Component status
- **Button (Large)** — light mode correct, dark mode now uses real Figma values. Button.jsx + Button.css not yet updated for new variants (secondary, outline, tonal) — carry-over for next session.

### Open items for next session
1. Extend `Button.jsx` + `Button.css` to support `type="secondary"` and `variant="outline"` / `variant="tonal"`.
2. Update `Button.stories.jsx` AllVariants to include secondary type rows and new variants.
3. Update `button-large-spec.md` with new variant/type table.
4. Pick next component to build.
