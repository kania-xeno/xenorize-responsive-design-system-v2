// CompactButton — pass-through re-export
// Per repo convention (see src/components/button/), consumers may import
// directly from "./CompactButton.jsx" — this file is provided for
// module-resolution convenience only. No barrel update required.
export { default } from "./CompactButton.jsx";
